Steps to run the code

1. Convert the shell script executing_code.sh into executable format by using chmod 777 executing_code.sh in the command line
2. Make sure the SPP_code.cpp and interior_ini.txt are in the same folder as executing_code.sh
3. Run the created executable by typing ./executing_code.sh in the command line.
